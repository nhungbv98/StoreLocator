var config = {
    map: {
        '*': {
            storelocator: 'Magepow_StoreLocator/js/googlestorelocator'
        },
        shim: {
            storelocator: {
                deps: ['jquery']
            }
        }
    }
};
